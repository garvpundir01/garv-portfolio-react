# Client-side routing: serve index.html for any path the static host can't find,
# so /my-story works on a hard refresh or a direct link.
# Used by Netlify and Cloudflare Pages; ignored by other hosts.
/*  /index.html  200
